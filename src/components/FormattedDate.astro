---
import { parseISO, format } from "date-fns";
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{format(parseISO(date.toISOString()), "LLL	dd, yyyy")}
</time>
